#include "utils.h"

#include <sstream>

using namespace std;


stringstream encode_base64(const istream&) {
    return stringstream{};
}

stringstream decode_base64(const istream&) {
    return stringstream{};
}
