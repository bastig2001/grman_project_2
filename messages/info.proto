syntax = "proto3";

message ShowFiles {
    oneof optional_options {
        string options = 1;
    } 
}

message FileList {
    repeated File files = 1;
}

message File {
    string file_name = 1;
    uint64 timestamp = 2;
    string signature = 3;
}
